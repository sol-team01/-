<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hac.mapper.BoardMapper">

	<select id="getList" resultType="com.hac.dto.boardDto.BoardDto">
	    select * from board where B_CATEGORY != '공지사항' 
	    order by B_NO
	    <choose>
	    	<when test='sort == 0'>
	    	desc
	    	</when>
	    	<otherwise>
	    	asc
	    	</otherwise>
	    </choose>
	    limit #{limitIndex}, 7
	</select>
	
	<select id="getNoticeList" resultType="com.hac.dto.boardDto.BoardDto">
		select * from board where B_CATEGORY = '공지사항' order by B_NO desc limit 3
	</select>
		
	<select id="searchList" resultType="com.hac.dto.boardDto.BoardDto" parameterType="com.hac.dto.boardDto.BoardSDto">
	    SELECT *
	    FROM board
	    <where>
	    	<choose>
	    		<when test='column.equals("B_TITLE")'>
	    			B_TITLE LIKE CONCAT('%', #{word}, '%')
	    		</when>
	    		<when test='column.equals("B_TEXT")'>
		             B_TEXT LIKE CONCAT('%', #{word}, '%')
	    		</when>
	    		<when test='column.equals("B_TITLE_OR_B_TEXT")'>
		             (B_TITLE LIKE CONCAT('%', #{word}, '%') OR B_TEXT LIKE CONCAT('%', #{word}, '%'))
	    		</when>
				<when test='column.equals("B_ID")'>
		             B_ID LIKE CONCAT('%', #{word}, '%')
				</when>
	    	</choose>
	    </where>
	    order by B_NO desc
	    LIMIT #{limitIndex}, 7;
	</select>

	<select id="searchListCount" resultType="int" parameterType="com.hac.dto.boardDto.BoardSDto">
	    select count(*) from board where 
	        <choose>
	    		<when test='column.equals("B_TITLE")'>
	    			B_TITLE LIKE CONCAT('%', #{word}, '%')
	    		</when>
	    		<when test='column.equals("B_TEXT")'>
		             B_TEXT LIKE CONCAT('%', #{word}, '%')
	    		</when>
	    		<when test='column.equals("B_TITLE_OR_B_TEXT")'>
		             (B_TITLE LIKE CONCAT('%', #{word}, '%') OR B_TEXT LIKE CONCAT('%', #{word}, '%'))
	    		</when>
				<when test='column.equals("B_ID")'>
		             B_ID LIKE CONCAT('%', #{word}, '%')
				</when>
	    	</choose>
	</select>
	
	<select id="totalContent" resultType="int">
		select count(*) from board where B_CATEGORY != '공지사항'
	</select>
	
	<select id="read" resultType="com.hac.dto.boardDto.BoardDto">
		select * from board where B_NO = #{bno}
	</select>
	
	<delete id="del">
		delete from board where B_NO = #{bno}
	</delete>
	
	<insert id="write">
		insert into board (B_ID, U_NO, B_TITLE, B_CATEGORY, B_TEXT) 
		values 
		(#{b_ID},
		#{u_NO},
		#{b_TITLE},
		#{b_CATEGORY},
		#{b_TEXT})
	</insert>
	
	<update id="hit">
		update board set b_hit = b_hit + 1 where b_no = #{bno}
	</update>
	
	<update id="replyCount">
		update board set b_REPLY_COUNT = b_REPLY_COUNT + 1 where b_no = #{bno}
	</update>
	
	<update id="delReplyCount">
		update board set b_REPLY_COUNT = b_REPLY_COUNT - 1 where b_no = #{bno}
	</update>
	
	<update id="modify">
		update board set B_ID = #{b_ID},
		B_TITLE = #{b_TITLE},
		B_CATEGORY = #{b_CATEGORY},
		B_TEXT = #{b_TEXT}
		where B_NO = #{b_NO}
	</update>

</mapper>